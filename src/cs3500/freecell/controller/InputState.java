package cs3500.freecell.controller;

/**
 * Enumeration InputState represents the only possible three states of a user input, which is set to
 * default as a sequence of characters with no space.
 */
public enum InputState {
  QUIT, CORRECT, INCORRECT;
}
